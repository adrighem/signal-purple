# PR #17: update actions/checkout to 7.0.1

- **Intent:** update `actions/checkout` from 7.0.0 to 7.0.1.
- **Scope:** four digest-and-comment replacements across CI, dependency review,
  and release workflows. Credentials remain disabled and permissions,
  triggers, and checkout inputs are unchanged.
- **Provenance:** the proposed digest matches the verified upstream v7.0.1 tag;
  the Dependabot commit is verified.
- **Risk:** low. The patch includes safer pull-request handling and escaping
  fixes, and CI exercised the updated action.
- **Checks:** CI and dependency review pass. PR CodeQL is neutral because the
  expected language configurations were not found.
- **Status:** merged as `8c426259` after explicit approval. Post-merge CI,
  Debian build/install/probe, CodeQL, and Release Please pass.
