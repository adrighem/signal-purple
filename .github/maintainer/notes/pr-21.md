# PR #21: update sha2 to 0.11.0

- **Intent:** update direct `sha2` from 0.10.9 to 0.11.0.
- **Scope:** direct manifest and lockfile changes. The old sha2 remains
  transitively, and `const-oid` is added through feature unification.
- **Risk control:** a separate `Refs #21` commit pins the persisted identifier
  for the all-zero group key to
  `3560c18a595af2d16e2297a210a7b429779e0da6d83411193cf692b4a1e137d7`.
  The value was independently reproduced from the pre-upgrade algorithm.
- **Repair:** rebased onto merged PR:18 and PR:20 and restored the marker in a
  separate `Refs #21` commit.
- **Status:** merged as `6160293a`. The final dependency graph retains
  transitive sha2 0.10.9 alongside direct sha2 0.11.0 and adds only
  dependency-free `const-oid`. PR and post-merge CI, Debian
  build/install/probe, CodeQL, dependency review, and Release Please pass.
