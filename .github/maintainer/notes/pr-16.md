# PR #16: update actions/upload-artifact to 7.0.1

- **Intent:** update the release build from `actions/upload-artifact` 6.0.0 to
  7.0.1.
- **Scope:** one digest-and-comment replacement in
  `.github/workflows/release-artifacts.yml`.
- **Provenance:** the proposed digest matches the verified upstream v7.0.1 tag;
  the Dependabot commit is verified.
- **Risk:** low-to-moderate. The workflow retains the default archived
  multi-file behavior, but pull-request CI does not execute the release-only
  upload path.
- **Checks:** CI and dependency review pass. PR CodeQL is neutral because the
  expected language configurations were not found.
- **Status:** merged as `f60b2d39` after explicit approval. Post-merge CI,
  Debian build/install/probe, CodeQL, and Release Please pass.
