# PR #18: update futures to 0.3.33

- **Intent:** update the futures family from 0.3.32 to 0.3.33.
- **Scope:** lockfile-only update of nine futures-family crates, with no new
  dependency nodes, features, scripts, or network behavior.
- **Value:** upstream fixes include soundness, exception-safety, stacked-borrows,
  and leak corrections.
- **Repair:** PR:22 added the marker guard. The Dependabot change was rebased
  onto it and a separate `Refs #18` commit restored the marker.
- **Status:** merged as `a020a2a4`. The final diff changed only the nine
  futures-family lock entries. PR and post-merge CI, Debian
  build/install/probe, CodeQL, dependency review, and Release Please pass.
